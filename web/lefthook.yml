pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{ts,tsx,js,jsx,json}"
      run: "bunx biome check --write {staged_files}"
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: "bunx commitlint --edit {1}"
